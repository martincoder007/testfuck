<?php

/*$json = file_get_contents('php://input');
$decodedjson = json_decode($json, true);
extract($decodedjson);

if(strpos("$email", "@") === false)
{
echo "error1";
exit(0);
}
if (strlen($email)<5 === true)
{
echo "error4";
exit(0);
}
if(strlen($password)<8 === true)
{
echo "error2";
exit(0);
}
if(strlen($password)>12 === true)
{
echo "error3";
exit(0);
}
$response = array('email' => "$email", 'password' => "$password");
echo json_encode($response);*/

echo "hello world";

?>
